# Old workflow — disabled
name: disabled
on:
  workflow_dispatch:
    inputs:
      dummy:
        description: 'disabled'
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
